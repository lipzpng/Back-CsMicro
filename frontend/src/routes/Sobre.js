import SobreContent from "../components/SobreContent"

export default function Sobre() {    
    return (
        <div>
            <main>
                <SobreContent />
            </main>
        </div>
    );
}
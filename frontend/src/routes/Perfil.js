import PerfilContent from "../components/PerfilContent";

export default function Perfil() {
    return (
        <div>
            <main>
                <PerfilContent />
            </main>
        </div>
    );
}